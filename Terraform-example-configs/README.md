## Do NOT use in production!
Some examples of the Terraform configurations I've experimented with. Not for production use! The code is inherently unsafe with clear text passwords in both configuration and output.

They are also not complete, as variables.tf and other separate files are missing 
